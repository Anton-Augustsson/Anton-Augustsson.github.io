import { ProjectCardDirective } from './project-card.directive';

describe('ProjectCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectCardDirective();
    expect(directive).toBeTruthy();
  });
});
